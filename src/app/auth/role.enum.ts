import { NONE_TYPE } from '@angular/compiler/src/output/output_ast';

export enum Role {
    // Aqui se pone una lista de enumaradores para los roles de los usuarios
    Admin = 'admin',
    User = 'user',
    None = ''
}
